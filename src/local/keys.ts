const prefix = 'issue.local';

export const localKeys = {
  issueProjects: `${prefix}.issueProjects`,
  issues: `${prefix}.issues`,
  settings: `${prefix}.settings`,
  syncMetadata: `${prefix}.syncMetadata`,
};
